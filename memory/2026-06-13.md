# 2026-06-13 工作日志

## 上午

### 1. 全局异常处理器（方案 4）
- 创建 `common/Result.java` — 统一响应包装类（code/msg/data）
- 创建 `common/ErrorCode.java` — 错误码枚举（200/400/401/403/404/405/500）
- 创建 `exception/BusinessException.java` — 业务异常类
- 创建 `exception/GlobalExceptionHandler.java` — 全局异常拦截
- 创建 `config/CorsConfig.java` — 跨域配置
- 修复 `Result.error()` 缺少三参数重载方法

### 2. 目录结构调整
- 删除 `repository` 和 `service/impl` 目录
- 创建 `mapper` 和 `resources/mapper` 目录
- 修正 `application.yml` 中 type-aliases-package 包名

### 3. Spring Security 配置
- 创建 `config/SecurityConfig.java` 放行所有接口（开发阶段）

### 4. 认证方案讨论
- 对比 Session vs JWT vs JWT+Security
- 确定渐进式路线：JWT → Spring Security → RBAC

### 5. MES 数据库设计路线图
- 创建 `memory/mes-learning-roadmap.md`
- 完整 6 周路线图，包含 20+ 张表，每张表所有字段都有中文注释
- 覆盖：产品、物料、BOM、工艺路线、工单、报工、领料、质检、追溯、设备、员工、报表
